[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vbot"
version = "0.1.9"
description = "A local-first agent harness: an async Python runtime giving AI agents a workspace, persistent state, host tool access, and server/WebUI/desktop/CLI accessors."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "The vBot Authors" }]
keywords = ["ai", "agent", "llm", "agentic", "fastapi", "svelte", "automation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
]
dependencies = ["httpx", "idna>=3,<4", "curl_cffi>=0.7,<1", "pyyaml", "croniter", "tzdata", "tzlocal>=5,<6", "beautifulsoup4", "pypdf>=4,<6", "sqlite-vec>=0.1.9,<0.2", "pathspec>=1.1"]

[project.scripts]
vbot = "cli.main:main"

[project.urls]
Homepage = "https://github.com/Vironnimo/vbot"
Repository = "https://github.com/Vironnimo/vbot"
Issues = "https://github.com/Vironnimo/vbot/issues"

[project.optional-dependencies]
server  = ["fastapi", "uvicorn[standard]", "websockets", "watchfiles", "python-telegram-bot[rate-limiter]", "discord.py>=2.7,<3", "python-multipart"]
cli     = ["psutil"]
desktop = ["pywebview", "openwakeword", "sounddevice", "webrtcvad"]
dev     = ["pytest", "pytest-asyncio", "ruff", "mypy", "hatchling", "pytest-xdist", "pytest-timeout", "respx", "fastapi", "uvicorn[standard]", "websockets", "watchfiles", "python-telegram-bot[rate-limiter]", "discord.py>=2.7,<3", "python-multipart", "psutil", "numpy"]

[tool.hatch.build.targets.wheel]
packages = ["core", "server", "cli", "desktop"]

[tool.pytest.ini_options]
addopts = "-n auto --timeout=30 --import-mode=importlib"
testpaths = ["tests"]
asyncio_default_fixture_loop_scope = "module"
asyncio_default_test_loop_scope = "module"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false

[[tool.mypy.overrides]]
module = ["openwakeword", "openwakeword.*", "sounddevice", "webrtcvad", "yaml"]
ignore_missing_imports = true
