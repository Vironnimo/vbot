[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vbot"
version = "0.3.3"
description = "A local-first agent harness: an async Python runtime giving AI agents a workspace, persistent state, host tool access, and server/WebUI/desktop/CLI accessors."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE", "THIRD_PARTY_NOTICES.md"]
authors = [{ name = "The vBot Authors" }]
keywords = ["ai", "agent", "llm", "agentic", "fastapi", "svelte", "automation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
]
dependencies = ["httpx", "idna>=3,<4", "curl_cffi>=0.7,<1", "pyyaml", "croniter", "tzdata", "tzlocal>=5,<6", "beautifulsoup4", "pypdf>=4,<6", "sqlite-vec>=0.1.9,<0.2", "pathspec>=1.1", "jsonschema>=4.25,<5", "websockets>=14,<17", "pyte>=0.8.2,<0.9", "pywinpty>=3,<4; sys_platform == 'win32'", "ptyprocess>=0.7,<1; sys_platform != 'win32'"]

[project.scripts]
vbot = "cli.main:main"

[project.gui-scripts]
vbot-autostart = "cli.autostart_management:windows_autostart_main"
vbot-desktop = "desktop.main:main"

[project.urls]
Homepage = "https://github.com/Vironnimo/vbot"
Repository = "https://github.com/Vironnimo/vbot"
Issues = "https://github.com/Vironnimo/vbot/issues"

[project.optional-dependencies]
server  = ["fastapi", "uvicorn[standard]", "watchfiles", "python-telegram-bot[rate-limiter]", "discord.py>=2.7,<3", "python-multipart", "av>=18,<19"]
cli     = ["psutil"]
desktop = ["pywebview", "pyopen-wakeword>=1.1,<2", "sounddevice", "webrtcvad-wheels>=2.0.14,<3"]
dev     = ["pytest", "pytest-asyncio", "ruff", "mypy", "types-jsonschema", "hatchling", "pytest-xdist", "pytest-timeout", "respx", "fastapi", "uvicorn[standard]", "watchfiles", "python-telegram-bot[rate-limiter]", "discord.py>=2.7,<3", "python-multipart", "psutil", "numpy", "av>=18,<19"]

[tool.hatch.build.targets.wheel]
packages = ["core", "server", "cli", "desktop"]

[tool.pytest.ini_options]
addopts = "-n auto --timeout=30 --import-mode=importlib"
testpaths = ["tests"]
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false

[[tool.mypy.overrides]]
module = [
    "ptyprocess",
    "pyopen_wakeword",
    "pyopen_wakeword.*",
    "sounddevice",
    "webrtcvad",
    "winpty",
    "yaml",
]
ignore_missing_imports = true
