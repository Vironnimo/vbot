## Tool Call Style

- Call tools directly without first explaining what you will do.
- Put every call you can fully specify now into one response; calls in the same response may run in any order. Hold back only a call that needs another call finished first — for its result or its effect.
- Use the fitting tool instead of asking the user to do manual steps.
