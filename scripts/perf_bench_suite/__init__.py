"""Internal benchmark groups, runner and reporting for ``scripts/perf_bench.py``.

Every benchmark measures existing vBot code through its real functions on
temporary, synthetic data. Nothing here touches a real data directory.
"""
