# OpenCode Go Provider

OpenAI-compatible gateway with per-Model protocol/request profiles across **three wires** (`/chat/completions`, Anthropic-compatible `/messages`, and OpenAI `/responses`).

## Interfaces

- Provider config: `resources/providers/opencode-go.json`
- Adapter selector: `opencode_go`
- Adapter class: `OpenCodeGoAdapter`
- Default runtime endpoint: OpenAI-compatible `POST /chat/completions`
- Alternate runtime endpoint for selected models: `POST /messages` through an internal `AnthropicCompatibleAdapter`
- Alternate runtime endpoint for selected models: `POST /responses` through the shared stateless Responses machinery (`core/providers/github_copilot_responses.py`, same shape the OpenRouter adapter uses for its GPT-5.6 ids)

## Runtime Behavior

- OpenAI-routed Assistant responses normalize readable Reasoning from the profiled inbound field and replay it as `reasoning_content`. The response carrier and historical carrier are independent: Kimi K3, Kimi K2.6, Hy3, and Hy4 currently respond with `reasoning` plus `reasoning_details`, but OpenCode's own compatible client sends readable history as `reasoning_content` and does not replay `reasoning_details`. `OpenCodeGoAdapter.reasoning_replay_fidelity()` therefore declares `readable_only` for the Chat route.
- **Protocol routing is DATA, not a hardcoded set.** The adapter routes each Model by `metadata.opencode_go.protocol` (`"anthropic"` -> internal Messages adapter, `"responses"` -> stateless Responses wire, `"openai"`/anything else -> default OpenAI `/chat/completions`), resolved through injected `model_lookup`. The endpoint returns bare ids with no protocol, so the facts live in `resources/models/opencode-go.overrides.json`. An unprofiled Model takes the safe OpenAI default and logs one warning per process. The then-current official 28-Model table was checked on 2026-09-10: responses -> `grok-4.6`, `gpt-5.6-luna`, `muse-spark-1.2-contributor`, `muse-spark-1.3-contributor`; openai -> `deepseek-flash`, `glm-5.3-flash`, `glm-5.3`, `glm-5.2`, `glm-5.1`, `kimi-k3`, `kimi-k2.7-code`, `kimi-k2.6`, `longcat-2.0`, `deepseek-v4-pro`, `deepseek-v4-flash`, `deepseek-v4-flash-vision-exp`, `mimo-v2.5`, `mimo-v2.5-pro`, `hy4-preview`, `hy3`; anthropic -> `minimax-m3`, `minimax-m2.7`, `minimax-m2.5`, `qwen3.8-max`, `qwen3.8-flash`, `qwen3.7-max`, `qwen3.7-plus`, `qwen3.6-plus`. Legacy ids still returned by `/models` retain profiles but are not part of that current official table. The 28-id baseline plus Union Alpha (verified 2026-09-16 below) is a registry regression test; new ids must be reconciled with the official endpoint table rather than inheriting the OpenAI fallback unnoticed.
- **Responses route mechanics:** the adapter reuses the shared `build_responses_payload` / `normalize_responses_response` / `iter_responses_sse_deltas_with_state` / `ResponsesStreamState` machinery. Requests are stateless - complete history as `input` every request, every original output item preserved for replay, `store: false`, structured in-band SSE error classification, and the provider-wide 429 subscription-limit classification applied to establishment failures. Effort rendering goes through `OpenCodeGoResponsesPolicy`; a Model with a documented `none` rung sends explicit `reasoning.effort: "none"`, because omission can select a reasoning-on Provider default. An always-reasoning Model's override fact `minimum_reasoning_effort` maps `none` to its cheapest supported rung, while another Model omits the reasoning object when no off rung exists. Provider config `defaults` merge under caller kwargs like on the Chat path; body-level session-routing kwargs are dropped because OpenCode Go receives cache affinity through `x-opencode-session`. Live verification on 2026-09-02 proved exact encrypted Reasoning item replay across three plain and Tool rounds for `gpt-5.6-luna`, `muse-spark-1.2-contributor`, and `muse-spark-1.3-contributor`; streaming produced terminal Usage and working Tool continuations for all three. GPT-5.6 Luna accepted `none` through `max` and reported `reasoning.context: "all_turns"`; both Muse versions accepted `minimal` through `xhigh` and have no verified off rung. A Muse 1.2 request with only 256 output tokens spent the allowance on Reasoning and emitted no message, while 1024 completed normally; inspect completion state and retry with a realistic budget before diagnosing a missing carrier or stream failure.
- OpenCode Go asks API clients to identify themselves. The bundled Provider config sends `User-Agent: vBot` on every wire. Every Chat request also sends `x-opencode-session` with an opaque `vbot-<prompt-cache-affinity>` value: one Prompt Epoch stays stable, a cache-compatible same-scope Session fork shares the value, and history edits or Compaction rotate it with the affinity. When Chat context is unavailable, the Adapter deterministically hashes Project/Agent/Session instead of exposing their raw ids. The header is request-only, is rebuilt alongside auth on retries, and is covered across `/chat/completions`, `/messages`, and `/responses` in streaming and non-streaming tests. Keep static identification in Provider config and dynamic conversation affinity in `OpenCodeGoAdapter.request_context_kwargs()` plus the compatible Adapter request-header hook.
- The internal Messages wire uses `x-api-key` with no prefix and `anthropic-version: 2023-06-01` while borrowing the outer adapter's HTTP client and sharing the exact token getter, runtime base URL, `model_lookup`, and debug recorder. The inner wire never closes the borrowed client; `OpenCodeGoAdapter` closes the one connection-scoped client exactly once.
- `send`, `stream`, `normalize_response`, and `wire_media_support` all use the same per-Model protocol decision. With `model_id`, response normalization routes by metadata; shape inference (`choices` = OpenAI, otherwise Messages) remains only for legacy callers that omit it. The Messages wire declares image media types only; the OpenAI wire declares images + WAV/MP3, and Chat still intersects that wire set with each Model's actual input modalities. PDF is not declared because OpenCode Go's gateway has not been verified for native PDF, even though the generic Messages encoder can shape a document block.
- When a catalog entry has a positive `max_output_tokens`, OpenCode Go uses it as request `max_tokens` unless the caller supplied `max_tokens`, `max_completion_tokens`, or `max_output_tokens`. The explicit-vs-ceiling logic now lives in the shared `OpenAICompatibleAdapter` base (`_apply_model_output_limit`; see `providers/request-policy.md` -> Request limits and context); this Adapter contributes only its flat vendor-prefixed candidate resolution (`_model_max_output_tokens` override + `_model_lookup_candidates`, matching `id`, the `::`-stripped id, and the post-`/` tail) and applies the resolution in `send`/`stream` so the ceiling is stamped before the request splits to the Anthropic sub-route as well.
- Both Tool wires preserve the canonical schema without Provider strict mode. The OpenAI route omits the field; a live `deepseek-v4-flash` request on 2026-07-31 accepted `strict: true`, but acceptance does not establish enforcement and does not change vBot's Provider-wide non-strict invariant. The Messages route also omits the field, and Runtime argument validation remains authoritative on both routes.

## Provider facts come from the models.dev section (rebuilt 2026-09-16)

The opencode-go endpoint returns **bare ids** - no context window, output cap, modalities, family, or reasoning info. But models.dev carries a per-provider **`opencode-go` section** with all of those, so refresh pulls them into the generated `opencode-go.json` (`discovery._enrich_provider_model` via `models_dev.provider_limits` / `provider_modalities` / `provider_family` / `provider_reasoning_supported`):

- **Limits**: the gateway's own `context.window` / `output` - which legitimately deviate from the lab (e.g. `glm-5` output **32768** vs the canonical 131072; `minimax-m3` **1000000/131072**). "Fill, don't overwrite": a provider that *did* report a limit keeps it.
- **Modalities**: widened to the models.dev set as a strict **superset** of what the endpoint reported (add, never drop) - so `minimax-m3`/`kimi`/`qwen*-plus` are correctly image/video-capable; `vision` is kept consistent.
- **Family** when the entry has none, and the bare **`reasoning: true`** flag (independent of a control ladder), so a Model the feed marks reasoning-capable is not flattened to `supported: false`. Where models.dev publishes `reasoning_options`, the typed control is stamped too; Models with no published control retain `{supported: true}` and use the Adapter's documented fallback behavior.

The **override** (`resources/models/opencode-go.overrides.json`) carries verified wire facts and corrections that cannot safely be inferred from a Provider-wide rule:

- **Replay hierarchy and per-Model protocol:** the root `reasoning_replay: "full_history"` is the Provider override, an optional top-level Model value can replace it, and `protocol` remains an independent metadata fact. Replay scope never lives inside `metadata.opencode_go`.
- **Response carrier:** `reasoning_response_field` records only the inbound Chat response field: `reasoning_content` for DeepSeek, GLM, LongCat, and MiMo; `reasoning` for Kimi K2.6/K3 and Hy3/Hy4. This is deliberately not the historical carrier, which the Chat serializer owns as `reasoning_content`. The metadata is repeated in the override because `metadata` is replaced wholesale at load. Responses-routed Models return reasoning items instead and carry no response-field profile.
- **DeepSeek V4.1 Flash controls:** the gateway id is `deepseek-flash`. `thinking_control: "toggle_with_effort"` renders `thinking.type` independently of the `low/high/max` effort ladder. `none` sends disabled without effort; enabled requests preserve the closest supported effort. An unspecified effort keeps the Provider default.
- **Kimi controls:** `thinking_control` distinguishes K2.5/K2.6 binary toggles from K2.7 always-on thinking; K2.6 adds `thinking_keep: "all"`. K3 uses `reasoning_effort` with the official `low/high/max` ladder and is always reasoning.
- **Always-reasoning minimum:** Grok 4.5, Grok 4.6, and Kimi K3 carry `minimum_reasoning_effort: "low"` so an Agent-selected `none` becomes the cheapest valid level instead of omission falling back to the Provider's high/max default.
- **Responses controls:** GPT-5.6 Luna exposes `none`, `low`, `medium`, `high`, `xhigh`, and `max`; the explicit `none` value is transported instead of omitted. Muse Spark 1.2 and 1.3 expose `minimal`, `low`, `medium`, `high`, and `xhigh`; `none` remains omitted because no off rung is documented or live-verified.
- **Provider-default Messages controls:** Union Alpha carries `thinking_control: "provider_default"`. The inner Messages Adapter omits thinking/effort controls for every Agent effort, and `describe_reasoning_render()` reports `default`. This is independent of the feed's positive Reasoning capability and does not disable or strip native historical Thinking blocks.
- **Privacy expiry:** OpenCode's current page still says its monthly DeepSeek zero-retention agreement was valid only through 2026-08-31. That date has passed; do not claim current DeepSeek ZDR until the Provider publishes a renewed term.

Since `metadata` is replaced **wholesale** by the highest layer at load (assembly contract), the override's `metadata.opencode_go` becomes the effective metadata.

## Reasoning Replay

- `reasoning_replay_policy()` uses the shared Model -> Provider -> system hierarchy. The Provider root is `full_history`; current official and legacy Models inherit it. Chat owns exact same-route history shaping.
- Re-verification on 2026-09-02 used Provider-reported input tokens, visible-content controls, real Provider-emitted Reasoning, and the exact vBot Adapter/history path. Positive exact later-Run or Tool-continuation transport was measured for `glm-5.3-flash`, `glm-5.3`, `glm-5.2`, `glm-5.1`, `kimi-k2.7-code`, `longcat-2.0`, `deepseek-v4-pro`, `deepseek-v4-flash`, `deepseek-v4-flash-vision-exp`, `mimo-v2.5`, `mimo-v2.5-pro`, `minimax-m3`, `qwen3.8-max`, `qwen3.8-flash`, `qwen3.7-max`, `qwen3.7-plus`, and `qwen3.6-plus`. Exact output-item replay across three plain and Tool rounds was also measured for Responses-routed `grok-4.6`, `gpt-5.6-luna`, `muse-spark-1.2-contributor`, and `muse-spark-1.3-contributor`. These Models must remain `full_history` even when a different supported request shape ignores history.
- Kimi K3 is a gateway-divergence case, not a no-replay Model. Moonshot's official contract requires complete Assistant messages including `reasoning_content` and Tool Calls for preserved-thinking history. OpenCode and Hermes send `reasoning_content`. Current OpenCode Go live requests on 2026-09-02 nevertheless ignored exact model-generated readable Reasoning, exact `reasoning_details`, and both together: all carrier cases had the same input count as the absent case, while visible history increased input and recalled the hidden values. vBot retains `full_history` and the documented readable carrier so it satisfies the upstream contract and becomes effective if the gateway is corrected; do not relabel Kimi K3 itself as replay-incapable.
- Kimi K2.6 has the same kind of current gateway divergence. Its direct API contract supports `thinking.keep: "all"` and requires historical `reasoning_content` during thinking Tool loops. OpenCode Go honored `thinking.type: "disabled"` but ignored exact generated, readable, and native-meta historical carriers in controlled token comparisons. vBot retains `thinking.keep: "all"` plus `full_history` so the upstream contract is correct and will work when the gateway transport is corrected.
- MiniMax M2.5 and M2.7 emitted exact signed Thinking blocks that vBot replayed on both live and persisted Tool continuations, but current OpenCode Go input accounting added zero tokens while visible controls counted. MiniMax's official Anthropic-compatible contract says to append the full response content, including Thinking, text, and Tool Use blocks, to preserve the reasoning chain. Treat the zero delta as a current gateway observation, retain exact block replay and `full_history`, and do not downgrade the Models from acceptance alone.
- Hy3 and Hy4 similarly ignored `reasoning_content` in current plain-history probes while visible controls counted. This is recorded as a current gateway observation, not encoded as a narrower Model policy, because the official compatible-client path still emits the carrier and the Provider may correct transport behavior.
- OpenAI-routed Assistant messages with non-empty canonical `reasoning` are replayed as `reasoning_content`; Chat route fidelity strips `reasoning_details` rather than preferring opaque meta. This matches current OpenCode and Hermes serialization and avoids sending duplicate or rejected reasoning classes.
- Anthropic-routed Models render replayed `reasoning_meta.content_blocks` through the inner `AnthropicCompatibleAdapter`, including its thinking-disabled guard. Responses-routed Models (`grok-4.6`, `gpt-5.6-luna`, `muse-spark-1.2-contributor`, `muse-spark-1.3-contributor`, plus legacy `grok-4.5`) replay whole original output items by design, including encrypted Reasoning, through the shared Responses input builder; Chat fidelity does not apply to them.
- Kimi K2.5/K2.6 render `thinking.type` from the Agent's on/off intent and never send generic `reasoning_effort`; K2.6 adds `keep: "all"` only while enabled. K2.7 always renders enabled and suppresses unsupported effort/disable fields. K3 and Grok use `reasoning_effort`, with `none` safely mapped to `low`.

### Union Alpha verification (2026-09-16)

- Exact Connection/Model: `opencode-go:api-key`, `union-alpha`, displayed as **Union Alpha Free**. The official Go endpoint table assigns `POST https://opencode.ai/zen/go/v1/messages` with `@ai-sdk/anthropic`; the override selects the existing inner Messages Adapter instead of the unprofiled Chat fallback. Authentication and session-affinity headers follow the existing Messages contract above.
- The refreshed models.dev Go record reports text/image input, text output, Tools, Reasoning supported, no Reasoning options, Context 262144 and output 131072. These limits remain catalog facts, not measured maximum capacities. OpenRouter's independent `stealth/union-alpha` record reports no exposed Reasoning capability; do not transfer that negative gateway fact to Go or infer the anonymous Model's internal architecture.
- Official Go documentation publishes no Union Alpha thinking toggle or effort ladder. Raw omission, `thinking: {type: enabled, budget_tokens: 2048}`, adaptive/summarized plus `output_config.effort: high`, and disabled requests all completed without `thinking`, `redacted_thinking`, signatures, or another Reasoning carrier. Their differing output-token counts do not prove that the controls affect the Model. The verified application profile leaves Reasoning to the Provider and makes no on/off promise, including for Agent effort `none`.

| Date | Provider/Connection | Model | Endpoint | Shape/control | Response/history carrier | A/B/C input tokens | Result | Official source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-16 | opencode-go:api-key | union-alpha | POST /messages | Raw arithmetic; enabled 2048 / adaptive high / disabled | None emitted; no generated state to replay | Not available | Input 49 in each case; output 66/265/85 for a short numeric answer; no observable control contract | https://dev.opencode.ai/docs/go/ (read 2026-09-16) |
| 2026-09-16 | opencode-go:api-key | union-alpha | POST /messages | Actual Adapter, streamed two Tool Calls; Provider default | Text + two tool_use blocks; no Thinking | Not available | Input/output 169/101; correlated argument deltas and terminal Usage completed | Same source/read date |
| 2026-09-16 | opencode-go:api-key | union-alpha | POST /messages | SQLite save/reopen, Chat history shaping, Tool continuation then later Run | Full Assistant text/Tool history; no Reasoning state | Not available | Input/output 246/462 then 583/3; Tool Results consumed and later answer correct | Same source/read date |
| 2026-09-16 | opencode-go:api-key | union-alpha | POST /messages | Actual Adapter, canonical PNG/JPEG/GIF/WebP | Text response; no Thinking | Not available | Four solid-color fixtures identified in order; input/output 87/131 | Same source/read date |

- Raw SSE also confirmed a zero-Usage `message_start`, overlapping argument deltas for two Tool Calls, and complete input/output Usage at terminal `message_delta`. The existing decoder handles that shape. Observed cache counters were zero; these short probes establish neither cache reuse nor its absence. A harder exact-cardinality optimization request with a 16384-token allowance exceeded the client's 180-second read timeout, so it supplies no Reasoning conclusion.
- **Replay remains unresolved, not `none`:** no live response supplied readable or opaque Reasoning for a valid absent/exact/visible A/B/C test. Preserve inherited `full_history` and the Messages wire's `meta_only` fidelity. A synthetic signed-block regression verifies local persistence/shaping/replay for every Agent effort; it is explicitly not evidence of Union Alpha accepting or consuming such a block. Future returned Thinking blocks must remain intact, without also sending plaintext Reasoning.
- Regression anchors: `tests/core/models/test_models_resources.py` protects independent gateway facts and the 29 verified endpoint profiles (the 2026-09-10 set plus Union Alpha); `tests/core/providers/test_opencode_go.py` protects control omission, render description, Usage, and native history preservation. Existing parametrized protocol, streaming, and route-isolation tests include Union Alpha. Other newly published ids, maximum Context/output, sampling efficacy, Tool Result media, and live Reasoning transport were not certified by this Union-only audit.

### Union Alpha reliability follow-up (2026-09-17)

The 2026-09-16 checks establish the tested wire features, not reliable completion of ordinary coding Runs. A reported no-answer incident remains unresolved; do not describe Union Alpha as operationally verified from the short probes above.

- Production trace inspection found both HTTP 503 (`api_error`, "Error from provider (Console Go): Upstream request failed: Endpoint is unavailable.") and HTTP 200 streams ending with zero bytes and no `message_stop`. Three complete Tool steps also reached vBot. The missing answer is therefore not evidence that the Model always receives the wrong endpoint or that vBot drops all output. Chat already retries these establishment/stream failures.
- Independent live requests with synthetic coding content and the 18 registered Tool schemas reproduced empty streams and streams ending during text deltas, often after roughly 18-33 seconds. Raw response capture confirms the terminal event is absent before decoding. Lowering the output allowance from 131072 to OpenCode's observed 32000, omitting temperature, changing cache markers or Tool definitions, and comparing HTTP/1.1 with HTTP/2 did not establish a reliable fix. These transient observations do not justify changing the catalog ceiling or canonical schemas.
- Installed OpenCode 2.0.5 completed small comparison tasks after retries. Its existing larger Union Alpha coding Session also advanced through many Tool steps, recorded repeated `provider.invalid-output` errors ("The provider response ended unexpectedly.", status 200), and ultimately ended with outcome `failed`. This confirms the reported difference in progress without establishing that OpenCode has a consistently working alternate route. Its resolved Model settings still name `https://opencode.ai/zen/go/v1` and the Anthropic Messages package. Capturing the actual Union Alpha requests (separately from title generation) confirmed the same `x-api-key` credential and `anthropic-version: 2023-06-01` as vBot.
- Replaying a captured OpenCode coding request through vBot's HTTP client produced complete `message_stop`/Usage responses, alongside transient failures. No deterministic request-field correction was isolated. Runtime routing, Reasoning policy, limits, and retry budgets remain unchanged. Next investigation should compare matched coding/Tool-continuation requests and recovery outcomes; a successful arithmetic response alone does not close this incident.

### DeepSeek V4.1 Flash verification (2026-09-10)

- Refresh follow-up (2026-09-11): models.dev moved the enriched record to `deepseek-v4.1-flash` while `/models` still advertises `deepseek-flash`. The existing alias now pins its previously verified reasoning and image capabilities plus the previous catalog limits in the override, preventing refresh from disabling Reasoning or losing Context. `test_gpt6_and_deepseek41_profiles_load` protects those retained facts. This does not transfer the alias's live evidence to the newly discovered id, which remains outside this Ollama-focused compatibility task.
- `/models` exposes `deepseek-flash`; models.dev supplies a 1,000,000-token Context window, 384,000-token output limit, image/text modalities, Tools, and the `low/high/max` reasoning ladder. The override supplies its display name, documented Chat endpoint, inbound `reasoning_content`, and independent thinking toggle. Context is a catalog fact; this audit live-tested text, image input, Reasoning controls, streaming, and Tool continuation.
- Raw requests accepted all three effort levels and the explicit thinking toggle. Adapter `none` emitted no Reasoning; omission stayed enabled. Streaming produced Reasoning, text, Tool Call deltas, terminal Usage, and cache-read accounting. Every gateway call carried `User-Agent: vBot` and stable `x-opencode-session` context.
- Real streamed Reasoning survived SQLite save/reopen and Chat history shaping as exact `reasoning_content`, without `reasoning_details`. With Tools, later-Run input tokens were absent/exact/visible = 345/446/447, identical through the Adapter and independent raw HTTP. Without Tools the same carrier was ignored (84/84/186), matching DeepSeek's documented no-Tools behavior. An exact Tool-loop probe with a follow-up User message counted 395/429/428; an immediate Tool-result-only continuation counted 514/514/630. Preserve `full_history`: positive consumption is verified, while the gateway's request-shape differences are observations rather than a reason to discard valid history.
- Additional live Adapter checks accepted canonical PNG/JPEG/GIF/WebP media and identified the generated solid-color fixtures correctly. Disabled thinking still produced two parallel Tool Calls. An oversized output request returned a Provider validation range of 1..393216; the catalog's conservative 384000 limit remains unchanged. Sampling `temperature: 0.2` plus `top_p: 0.9` was accepted, but acceptance does not prove both controls are effective; upstream documents ignored temperature and a 0.95 minimum top-p.
- Regression anchors: `tests/core/models/test_models_resources.py`, `tests/core/providers/test_opencode_go.py`, and `tests/core/providers/test_reasoning_route_switch_conformance.py` cover the exact id, endpoint, effort/toggle pairing, persisted carrier, and route isolation. `tests/scripts/test_probe_reasoning_replay_exact.py` protects the probe's required client/session headers.

## Catalog Integrity

The gateway's `/models` response is retained verbatim in `opencode-go.raw.json`, but the usable projection applies `ProviderConfig.catalog_exclusions`. `hy3-preview`, `mimo-v2-omni`, and `mimo-v2-pro` remain excluded legacy ids; current official `hy4-preview`, `mimo-v2.5`, and `mimo-v2.5-pro` are included. The full refresh returned 36 raw ids on 2026-09-10, while the official endpoint table names 28 current Models and all 28 were present. Extra ids are gateway availability, not current protocol documentation; the `omen-alpha` id has no verified protocol override.

## Prompt Caching

Prompt caching works on every current route and is accounted by vBot's Adapter path for the Models probed on 2026-07-09. OpenCode's compatible client contract and Go guidance were re-checked on 2026-09-03: `x-opencode-session` supplies stable gateway routing/cache affinity, while caching behavior and counters remain wire-specific below. Do not infer direct-xAI `x-grok-conv-id` support for Grok through this gateway without a separate probe.

- **OpenAI route** (`/chat/completions`): caching is **automatic upstream** - vBot sends no cache directive. Reads come back as `prompt_tokens_details.cached_tokens` and are read by the shared `_openai_cached_prompt_tokens` (`openai_compatible.py`) into `usage.cache_read_tokens`. Cold miss on the first turn, then near-full-prefix reads while the prefix is reused; a unique prefix per turn stays at ~0 (control). Verified: `deepseek-v4-flash` (0 -> 2304 -> 8064...), `kimi-k2.6` (cold 3 -> 7937). **`glm-5` caches but the gateway reports `prompt_tokens_details` as `null` on many turns** - vBot then honestly reports no read; this is an upstream reporting gap, not a vBot bug (`_openai_cached_prompt_tokens` tolerates the `null`).
- **Anthropic route** (`/messages`): OpenCode Go explicitly enables the compatible wire's `cache_control` breakpoints, and those markers **are effective on this gateway**. With markers on, the cold turn writes the whole prefix (`cache_creation_input_tokens` = full prefix) and later turns read it (`cache_read_input_tokens`); with markers off on a fresh prefix the write disappears and reads shrink or delay - verified on `qwen3.7-plus` (read 8480 with markers vs 6272 without) and `minimax-m2.7`. Some upstreams (`minimax-m3`) auto-cache regardless of markers, so the markers are redundant-but-harmless there. Counts are folded into `cache_read_tokens`/`cache_write_tokens` by `apply_anthropic_cache_usage`.
- **Streaming** surfaces the same reads on the OpenAI route and for `minimax-m2.7`/`minimax-m3`. OpenCode Go's MiniMax M3 Messages stream was observed on 2026-08-14 to report zero Usage at `message_start` and the complete fresh-input, cache-read, output, and Thinking counters only in the terminal `message_delta`; the shared compatible Messages decoder therefore prefers a usable terminal input/cache snapshot over the start snapshot. In an earlier probe `qwen3.7-plus`'s streaming `message_start` did not expose the cache breakdown (a gateway streaming detail), so absent counters remain unknown rather than being reconstructed as measured Provider values.

## Subscription Limits

- OpenCode Go overload/throttling and exhausted subscription/account allowance can both arrive as HTTP 429. Both Wires inspect the structured error detail before the shared status policy: stable limit identifiers (`GoUsageLimitError`, `FreeUsageLimitError`, `insufficient_quota`) and unambiguous monthly-limit, available-balance, quota, budget, or billing-hard-limit phrases raise a non-retryable Provider error immediately. Other 429 responses remain `ProviderRateLimitError`, retain `Retry-After`, and use the shared bounded retry policy.
- Keep this classification OpenCode-specific. A status-only global 429 change would disable valid transient retries for unrelated Providers, while broad error-word matching could misclassify recoverable throttling.

## Constraints & Gotchas

- Keep OpenCode-specific route selection and Chat `readable_only` fidelity in `OpenCodeGoAdapter`. The generic OpenAI-compatible Adapter owns serialization mechanics; do not infer the outgoing history carrier from `reasoning_response_field`, do not send both readable and opaque classes, and do not add history-wide Reasoning strips - Chat owns history shaping.
- Keep the compatible Messages owner borrowed and provider-neutral; do not replace it with the concrete native `AnthropicAdapter`, which would import native discovery, sampling, PDF, and cache defaults into OpenCode Go.
- Constructor signature intentionally matches runtime adapter factory injection, including optional `model_lookup` and `debug_recorder`.

## References

Read these only when your task matches - not by default.

- Adding, changing, or re-validating an endpoint, Reasoning control, response carrier, history carrier, replay scope, Tool behavior, Usage, media, or request limit -> `providers/compatibility-verification.md`
- Re-validating the current OpenCode Go Model/endpoint table or client-identification/session-affinity requirement -> `https://dev.opencode.ai/docs/go/` and `https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/session/llm/request.ts` (read 2026-09-03)
- Re-validating DeepSeek V4.1 Flash controls or Tool-history replay -> `https://api-docs.deepseek.com/guides/thinking_mode/` (read 2026-09-10)
- Re-validating GPT-5.6 Luna's Reasoning levels, all-turns context, or Responses contract -> `https://developers.openai.com/api/docs/models/gpt-5.6-luna` and `https://developers.openai.com/api/docs/guides/latest-model` (read 2026-09-02)
- Re-validating Kimi K2.6 preserved Thinking or Tool-loop history -> `https://platform.kimi.ai/docs/guide/kimi-k2-6-quickstart` and `https://platform.kimi.ai/docs/api/chat` (read 2026-09-02)
- Re-validating MiniMax M2.5/M2.7 Thinking history -> `https://platform.minimax.io/docs/api-reference/text-anthropic-api` and `https://platform.minimax.io/docs/api-reference/text-openai-api` (read 2026-09-02)
